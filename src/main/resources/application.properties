quarkus.application.name=gbuilder
quarkus.banner.enabled=false

# CLI-only — no HTTP server
quarkus.http.host-enabled=false

# Embedded graph store (auto-created under <codebase>/.gbuilder/graph.db)
gbuilder.graph.store-path=.gbuilder/graph.db

# Default parser backend when not specified on CLI: jparser | treesitter
gbuilder.parser.backend=jparser

# Parallel parse (virtual threads) and chunked SQLite writes
gbuilder.parse.parallel-threshold=16
gbuilder.parse.max-parallelism=0
gbuilder.graph.persist-batch-size=50
gbuilder.graph.edge-batch-size=500

# LangChain4j (embeddings only)
quarkus.langchain4j.openai.api-key=${OPENAI_API_KEY:sk-placeholder}
quarkus.langchain4j.openai.embedding-model.model-name=text-embedding-3-small
%dev.quarkus.langchain4j.openai.enable-integration=false

# Logging
quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] %s%e%n
quarkus.log.level=WARN
quarkus.log.category."dev.shaaf.gbuilder".level=INFO

# Embeddings (opt-in)
gbuilder.embedding.enabled=${GBUILDER_EMBEDDING_ENABLED:false}
gbuilder.embedding.batch-size=50
gbuilder.embedding.parallel-threshold=8
gbuilder.embedding.max-parallelism=0
gbuilder.embedding.similarity-threshold=0.80
gbuilder.embedding.query-min-similarity=0.65

# Analyzer rules
# gbuilder.analyzer.rules-dir=/path/to/custom/rules

%test.gbuilder.graph.store-path=target/test-graph.db
%test.quarkus.langchain4j.openai.enable-integration=false
